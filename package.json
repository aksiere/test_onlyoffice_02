{
	"scripts": {
		"test": "npx vitest",
		"postinstall": "npx --yes @rollup/rollup-linux-x64-gnu || true"
	},
	"dependencies": {
		"vitest": "^3.2.4"
	},
	"devDependencies": {
		"puppeteer": "^24.21.0"
	}
}